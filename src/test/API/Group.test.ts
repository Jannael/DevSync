describe('/group/v1/', () => {
	// const api = '/group/v1/'
	describe('/get/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
	describe('/create/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
	describe('/update/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
	describe('/delete/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
	describe('/join/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
	describe('/quit/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
})
