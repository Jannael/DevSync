describe('/member/v1/', () => {
	// const api = '/member/v1/'
	describe('/get/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
	describe('/update/role/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
	describe('/remove/', () => {
		test('good request')
		describe('error request', () => {
			for (let i = 0; i < 10; i++) {
				test('Forbidden: Missing required header')
			}
		})
	})
})
