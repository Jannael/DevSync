<div class="flex items-center justify-end">
  <button
    id="theme-toggle"
    class="hover:bg-surface-raised focus:ring-accent flex items-center justify-center rounded-lg p-2 transition-colors focus:outline-none focus:ring-2"
    aria-label="Toggle theme"
  >
    <span id="theme-icon" class="text-text">
      <!-- Icon injected by script -->
    </span>
  </button>
</div>

<script>
  const themeIcon = document.getElementById('theme-icon') as HTMLSpanElement
  const themeToggle = document.getElementById('theme-toggle') as HTMLButtonElement

  const sunIcon = `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none" /><path d="M8 12a4 4 0 1 0 8 0a4 4 0 1 0 -8 0" /><path d="M3 12h1m8 -9v1m8 8h1m-9 8v1m-6.4 -15.4l.7 .7m12.1 -.7l-.7 .7m0 11.4l.7 .7m-12.1 -.7l-.7 .7" /></svg>`
  const moonIcon = `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none" /><path d="M12 3c.132 0 .263 0 .393 0a7.5 7.5 0 0 0 7.92 12.446a9 9 0 1 1 -8.313 -12.454l0 .008" /></svg>`

  function getTheme() {
    return (
      localStorage.getItem('theme') ??
      (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light')
    )
  }

  function updateIcon() {
    const isDark = document.documentElement.classList.contains('dark')
    themeIcon.innerHTML = isDark ? moonIcon : sunIcon
  }

  function applyTheme(theme: string) {
    document.documentElement.classList.toggle('dark', theme === 'dark')
    localStorage.setItem('theme', theme)
    updateIcon()
  }

  // Initialize icon
  updateIcon()

  // Toggle on click
  themeToggle.addEventListener('click', () => {
    const currentTheme = getTheme()
    applyTheme(currentTheme === 'dark' ? 'light' : 'dark')
  })
</script>

<style>
  #theme-toggle {
    color: var(--color-text);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
  }

  #theme-toggle:hover {
    background: var(--color-surface-raised);
  }

  #theme-toggle:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }
</style>
